# Copy this to .env and adjust for your environment.
# Defaults (localhost) are used when these are unset.

# PostgreSQL (ParadeDB) connection string
# PG_DSN=postgresql://127.0.0.1:5432/codebase

# Ollama host
# OLLAMA_HOST=http://127.0.0.1:11434
